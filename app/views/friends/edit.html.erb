<h1 class="pb-2 pt-3">Editing Friend</h1>

<%= render 'form', friend: @friend %>

<%= link_to 'Show', @friend, class: "btn btn-secondary btn-sm mt-2" %>
<%= link_to 'Delete', @friend, method: :delete, data: { confirm: 'Are you sure?'}, class: "btn btn-danger btn-sm mt-2" %>
<%= link_to 'Back', friends_path, class: "btn btn-dark btn-sm mt-2" %>

